coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 60%
      fepack-image:
        target: 100%
        threshold: 60%

comment:
  layout: "header, reach, diff, flags, components"

component_management:
  individual_components:
    - component_id: fepack-image
      name: "@fepack/image"
      paths:
        - packages/image
